---
layout:     post
title:      机器学习知识点总结(一)
subtitle:   第一、二章
date:       2020-07-21
author:     Adam
header-img: img/post-bg-unix-linux.jpg
catalog: true
tags:
    - Machinelearning
	- 面试
---


> 本文首次发布于 [Adam's Blog](http://adamfocus.github.io), 作者 [@AdamFocus](http://github.com/AdamFocus) ,转载请保留原文链接.

## 第一章 绪论

### 概念：

1. 机器学习：在计算机上通过数据产生模型的算法

2. 模型：全局性结果

   模式：局部性结果

3. 数据 

   记录

   数据集data set：记录的集合

   示例 instance 样本 sample：每条记录

   属性attribute特征feature 

   属性值

   属性空间，样本空间，输入空间：属性张成的空间

   特征向量

   维数：属性个数

   版本空间：与训练集一致的假设集合

4. 分类:预测离散值

   回归：预测连续值

5. 监督学习：数据有无标注

   无监督学习：

6. 泛化：模型适用于新样本的能力

7. 独立同分布iid：获取的每个样本都是独立采样获取的

8. 归纳：从特殊到一般的泛化

   演绎：从一般到特殊的特化

9. 假设空间：

10. 归纳偏好：学习算法自身在假设空间中对假设进行选择的启发式

11. 奥卡姆剃刀：若有多个假设与观察一致，则选最简单的那一个

12. NFL定理：当所有问题出现的机会相同或同样重要，不管是什么算法，期望性能相同，总误差与学习算法无关

13. 机器学习发展历程：

    - 推理期：赋予机器逻辑推理能力

    - 知识期：赋予机器知识

    - 机器学习：

      - 机械学习：

      - 示教学习、类比学习：从指令中学习

      - 归纳学习：从样例中学习

        - 符号主义学习：决策树、基于逻辑的学习

        - 基于神经网络的连接主义：

        - 统计学习：

### 题目：

1. 根据样例求版本空间：

   首先根据属性和属性取值找出所有的假设

   假设数据集有n种属性，第i个属性可能的取值有$$t_i$$种，加上该属性的泛化取值(*)，所以可能的假设有$$\prod (t_i+1)$$。再用空集表示没有正例，假设空间中一共$$\prod (t_i+1)+1$$种假设。

   再根据给出的样例对假设空间进行删除



## 第二章 模型评估与选择

### 概念：

1. 错误率：分类错误的样本数占总数比例

   精度：1-错误率

2. 误差：训练误差、泛化误差

3. 过拟合：

   欠拟合：

4. 训练集测试集切分方法：

   切分时要尽量保持分布的一致性

   分层采样：保留类别比例的采样

   - 留出法：

     直接将数据集D切分成两个互斥的集合

     使用留出法时通常采用若干次随机划分、重复进行实验评估后取平均值作为留出法的评估结果

   - 交叉验证法：

     将数据集分层采样，切分成k个大小相似的互斥子集，每次用k-1个子集的并集作为训练集，余下的那个子集作为测试集，则可以获得k组用于训练和测试。

     在划分成k个子集通常随机使用不同的划分重复p次，即p次k折交叉验证。

   - 留一法

     k=样本总数

   - 自助法

     使用自助采样法，有放回的进行采样

     在数据集较小有用，同时可以在初始数据集产生较多训练集，有利于集成学习，但改变了原始数据集的分布，引入估计偏差

     > 样本在m次采样不被采到的概率是1/e=0.368
     >
     > 包外估计

5. 验证集：用于模型评估与选择

6. 性能度量:

   - 均方误差MSE：

     $$E(f;D)=\frac {1}{m}\sum_{i=1}^{m}(f(x_i)-y_i)^2$$

   - 错误率、精度

   - 准确率precision和召回率recall

     TP/FP/TN/FN

     $$P=\frac{TP}{TP+FN}$$

     $$R=\frac{TP}{TP+FN}$$

     二者通常相互矛盾，一个高一个则低

   - PR曲线、PR图

     以查全率为x轴，查准率为y轴，将所有样例按照预测结果排序，最可能是正例的样本在前。按这个顺序逐个将样本进行预测，计算此时的查全率和查准率。

     若一个学习器的PR曲线被另一个曲线包住，则可以断言后者性能优于前者。

     ![U5HTRH.png](https://s1.ax1x.com/2020/07/21/U5HTRH.png)

     选择截断点，注重查准率则截断点靠前，注重查全率则靠后

   - 平衡点BEP

     查准率=查全率的点

   - F1值：

     $$F1=\frac{2*P*R}{P+R}=\frac{2*TP}{样例总数+TP-TN}$$

     基于查全率查准率的调和平均

     $$F_β=\frac{(1+β^2)*P*R}{(β^2*P)+R}$$

     可以通过β调整二者的不同偏好，β>0度量了查全率对查准率的相对重要性，β>1时查全率有更大影响，β<1查准率有更大影响。

   - 宏查全率、宏查准率、宏F1值:

     在各个矩阵上求得的P/R/F1计算平均值得到macro-P/macro-R/macro-F1

     微查全率、微查准率、微F1值:

     先求平均再计算P/R/F1

   - ROC

     ROC曲线（受试者工作特征曲线）以TPR正真例率为纵轴，横轴为FPR假正例率。

     对角线为随机猜测模型

     $$TPR=\frac{TP}{TP+FN}$$
     
     $$FPR=\frac{FP}{TN+FP}$$
     
     ![U5HbQA.png](https://s1.ax1x.com/2020/07/21/U5HbQA.png)
     
     ROC曲线的绘制:
     
     根据学习器预测结果进行排序，首先设置所有样例预测为反例，则为(0,0)。依次将每个样例设为正例，并计算绘制点，依次相连。
     
     若一个ROC曲线将另一个ROC曲线完全包住，则前者性能更好。
     
   - AUC

     ROC曲线下的面积

     表现的是样本预测的排序质量，与排序误差$$l_{rank}$$有紧密关系

     $$l_{rank}=1-AUC$$
     
   - 代价敏感错误率和代价曲线

     曲线横轴为取值为[0,1]的正例概率代价 ，纵轴为[0,1]的归一化代价

     通过ROC曲线上每一个点绘制(0,FPR)到(1,FNR)的线段，所有线段下的总面积为期望总体代价

     ![UIZVJg.jpg](https://s1.ax1x.com/2020/07/21/UIZVJg.jpg)

7. 非均等代价：不同任务的损失计算不一样

8. 统计假设检验：

   解释算法泛化性能

   - 二项检验

   - t检验

   - 交叉验证t检验

     

   - McNemar检验

   - Friedman检验

   - Nemenyi后续检验

9. 偏差方差分解

   泛化误差可以分解为偏差、方差和噪声之和。

   $$E(f;D)=bias^2(x)+var(x)+ε^2$$

   - 偏差度量了算法的期望预测和真实结果的偏离程度，刻画了算法本身的拟合能力
   - 方差度量了同样大小的训练集的变动导致的学习性能变化，刻画了数据扰动带来的影响。
   - 噪声表达了当前任务任何算法达到的期望泛化误差下界，刻画了问题本身的难度。

10. 偏差方差窘境：偏差和方差是有冲突的

### 问题：
1. 解决过拟合、欠拟合的方法